# Ninav - Environment Configuration
# Copy this file to .env and customize for your environment

# ====================
# Backend Configuration
# ====================

# Path to your photo directory (absolute or relative)
# This directory will be scanned for images
IMAGE_ROOT=./sample_images

# Database URL (SQLite by default)
# For PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./data/faces.db

# CORS Origins (comma-separated for multiple origins)
# For production, specify exact origins instead of "*"
CORS_ORIGINS=["http://localhost:5180","http://localhost:5173"]

# Face recognition settings
FACE_SIMILARITY_THRESHOLD=0.6
FACE_SCAN_BATCH_SIZE=8

# GPU Worker URL (optional - for accelerated face detection)
# Uncomment and set to your GPU worker address if available
# GPU_WORKER_URL=http://192.168.1.100:8001

# ====================
# Frontend Configuration
# ====================

# Backend API base URL
VITE_API_BASE=http://localhost:8080/api

# Proxy target for development
VITE_PROXY_TARGET=http://backend:8000

# ====================
# Docker Configuration
# ====================

# Host directory for images (for docker-compose volume mount)
# Uncomment and set to your photos directory
# HOST_IMAGE_DIR=/path/to/your/photos
